<!DOCTYPE html>
<html lang="{{ site.locale }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" href="/assets/img/uzh_shortcut_icon_114.png" type="image/png"/>
    <title>{{ metaTitle }}</title>
    <meta name="description" content="{{ metaDescription }}">
    {% metaRobots %}
    <link rel="stylesheet" href="/main.css">

    <link rel="canonical" href="https://www.ovl.design"/>

    <meta property="og:type" content="article">
    <meta property="og:title" content="{{ metaTitle }}">
    <meta property="og:url" content="{{ siteUrl }}">
    <meta property="og:image" content="{{ site.metaImage }}">
    <meta property="og:image:type" content="image/png"/>
    <meta property="og:image:width" content="1478"/>
    <meta property="og:image:height" content="831"/>

    {# <meta property="og:site_name" content="{{ site.name }}"> #}
    <meta property="og:description" content="{{ metaDescription }}"/>

    <!-- twitter -->
    {# <meta name="twitter:card" content="summary">
  <meta name="twitter:site" content="{{ site.twitter }}">  #}
    <meta name="twitter:title" content="{{ metaTitle }}">
    <meta name="twitter:description" content="{{ metaDescription }}">
    <meta name="twitter:image" content="{{ site.metaImage }}">

    <meta property="x-website-version" content="{{ site.version }}"/>
  </head>
  <body class="{{ templateClass }}">
    <a href="#main" class="skip-link js-skip-link">{{ site.strings.skip_to_content }}</a>
    <div>
      <hr>
      <p>Things looking a little rough? This project is a <a href="https://ethanmarcotte.com/wrote/let-a-website-be-a-worry-stone/">worry stone</a>. I will continously port features from my <a href="https://www.ovl.design">current site</a>. <a href="https://github.com/ovlb/home-11ty">The code</a> is on GitHub.</p>
        <hr>
    </div>
    {% if hideSiteHeader !== true %}
      {% include "components/site-header.njk" %}
    {% endif %}
    {{ content | safe }}
    {# {% include "partials/footer.njk" %} #}
    <script>
      var $main = document.getElementById('#main')
      var $skipLink = document.querySelector('.js-skip-link')
      var $footer = document.querySelector('.js-site-footer')
      // reset tabindex if script is running
      $main.removeAttribute('tabindex')

      $skipLink.addEventListener('click', function (evt) {
        evt.preventDefault()

        $main.setAttribute('tabindex', '0')
        $main.focus()
        $main.removeAttribute('tabindex')
      })

      // as the footer is statically positioned, we need to ensure it becomes visible if focus moves inside of it
      $footer.addEventListener('focusin', function () {
        $footer.scrollIntoView()
      })
    </script>
  </body>
</html>