<!DOCTYPE html>
<html lang="{{ site.locale }}">
  {% set metaTitle = site.title + ' « ' + site.description %}
  {% if title %}
    {% set metaTitle = title + ' « ' + site.title %}
  {% endif %}
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ metaTitle }}</title>

    <link rel="apple-touch-icon" sizes="180x180" href="/img/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/img/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/img/favicon/favicon-16x16.png">
    <link rel="manifest" href="/img/favicon/site.webmanifest">
    <link rel="mask-icon" href="/img/favicon/safari-pinned-tab.svg" color="#b00000">
    <link rel="shortcut icon" href="/img/favicon/favicon.ico">
    <meta name="msapplication-TileColor" content="#f8f2e3">
    <meta name="msapplication-config" content="/img/favicon/browserconfig.xml">
    <meta name="theme-color" content="#f8f2e3">

    <meta name="description" content="{{ metaDescription }}">
    {% metaRobots %}
    {# <link rel="stylesheet" href="/css/main.css"> #}

    <style>{{ css.main | safe }}</style>
    {% if pageCSS %}
      <style>{{ css[pageCSS] | safe }}</style>
    {% endif %}

    <link rel="preconnect" href="https://static.owlish.dev" />
    <link rel="canonical" href="https://www.ovl.design"/>

    <meta property="og:type" content="article">
    <meta property="og:title" content="{{ metaTitle }}">
    <meta property="og:url" content="{{ siteUrl }}">
    <meta property="og:image" content="{{ site.metaImage }}">
    <meta property="og:image:type" content="image/png"/>
    <meta property="og:image:width" content="1478"/>
    <meta property="og:image:height" content="831"/>

    {# <meta property="og:site_name" content="{{ site.name }}"> #}
    <meta property="og:description" content="{{ metaDescription }}"/>

    <!-- twitter -->
    {# <meta name="twitter:card" content="summary">
  <meta name="twitter:site" content="{{ site.twitter }}">  #}
    <meta name="twitter:title" content="{{ metaTitle }}">
    <meta name="twitter:description" content="{{ metaDescription }}">
    <meta name="twitter:image" content="{{ site.metaImage }}">

    <meta property="x-website-version" content="{{ site.version }}"/>
  </head>
  <body class="{{ templateClass }}">
    <a href="#main" class="skip-link js-skip-link">{{ site.strings.skip_to_content }}</a>
    <div>
      <p class="worry-stone">Things looking a little rough? This project is a <a href="https://ethanmarcotte.com/wrote/let-a-website-be-a-worry-stone/">worry stone</a>. I will continously port features from my <a href="https://www.ovl.design">current site</a>. <a href="https://github.com/ovlb/home-11ty">The code</a> is on GitHub.</p>
    </div>
    {% if hideSiteHeader !== true %}
      {% include "components/global/site-header.njk" %}
    {% endif %}
    {% block content %}
    {% endblock %}
    {% if usesBlock !== true %}
      {{ content | safe }}
    {% endif %}
    {% if hideSiteHeader !== true %}
      {% include "components/global/site-footer.njk" %}
    {% endif %}
    <script type="module" src="/js/main.js"></script>
  </body>
</html>