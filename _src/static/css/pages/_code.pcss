.content-code {
  --clr-accent: #28caf0;
  --clr-red: #f76050;
  --clr-bg: #222325;

  background-color: var(--clr-bg);
  color: var(--clr-light);
  font-family: var(--fonts-mono);
  padding: 4vmin;
}

.content-code__intro {
  margin-bottom: 2vmin;
}
